/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.postimg.cc", "bit.ly"],

  },
};

export default nextConfig;
/* 
// next.config.js
module.exports = {
  images: {
    domains: ['i.postimg.cc'],
  },
};

 */
